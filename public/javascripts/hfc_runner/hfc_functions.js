

// A test: I want to know if it is possible to use the with command to suck in symbosl

var HfcFunctions {
	circle: function( x, y, r ) {
		alert( "circle " + x + " " + y + " " + r );
	},

	box: function( x, y, w, h ) {
		alert( "circle " + x + " " + y + " " + w + " " + h );
	}
};

